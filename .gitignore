# Node modules (huge folder, don’t ever push)
node_modules/

# Environment variables (never push secrets)
.env

# OS specific files (trash)
.DS_Store
Thumbs.db

# Log files
*.log

# Debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build / Cache
dist/
build/
.cache/

# IDE/Editor configs (optional, don’t need to share)
.vscode/
.idea/

# Package lock files (optional — only ignore if you want)
# package-lock.json
# yarn.lock
